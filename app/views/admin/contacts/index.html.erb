<h3>Список связавшихся с нами</h3>
<hr/>
<%= render 'admin/nav_links' %>

<div class="table-responsive">
<table class="table table-striped table-sm">
  <thead>
    <tr>
      <th class="" scope="col">Id</th>
      <th class="" scope="col">Почтовый адресс</th>
      <th class="" scope="col">Сообщение</th>      
      <th class="" scope="col">Дата создания</th>
      <th class="shrink" scope="col"></th>
    </tr>
  </thead>
  <tbody>
  	<% @contacts.each do |contact| %>
    <tr>
      <td><%= contact.id %></td>
      <td><%= contact.email %></td>
      <td><%= contact.message %></td>
      <td><%= contact.created_at %></td>
      <td><div class="btn-group btn-group-sm align-content-start" role="group"><%= link_to(admin_contact_path(contact), title: "Показать", class: "btn btn-outline-primary", data: { toggle: 'tooltip', placement: 'top', original_title: 'Показать' }) do %>
          <%= icon("info-circle", class: "bi bi-info-circle") %>
          <% end %>

          <%= link_to(edit_admin_contact_path(contact), title: "Редактировать", class: "btn btn-outline-warning", data: { toggle: 'tooltip', placement: 'top', original_title: 'Редактировать' }) do %>
          <%= icon("pencil-square", class: "bi bi-pencil-square") %>
          <% end %>

          <%= link_to(admin_contact_path(contact), method: :delete, title: "Удалить", class: "btn btn-outline-danger", data: { toggle: 'tooltip', placement: 'top', original_title: 'Удалить', confirm: 'Вы действительно хотите удалить объект?' }) do %>
          <%= icon("backspace-reverse", class: "bi bi-bi bi-backspace-reverse") %>
          <% end %>
        </div>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>