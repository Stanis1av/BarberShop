<h3>Парикмахеры</h3>
<hr/>
<ul class="nav nav-tabs">
  <li class="nav-item">
    <%= link_to(new_admin_service_path, title: "Создать", class: "nav-link active", data: { toggle: 'tooltip', placement: 'top', original_title: 'Создать' }) do %>
        <%= icon("plus", class: "bi bi-plus") %> Создать
    <% end %>
  </li>
</ul>
<div class="table-responsive">
<table class="table table-striped table-sm">
  <thead>
    <tr>
      <th class="" scope="col">Id</th>
      <th class="" scope="col">Имя услуги</th>
      <th class="" scope="col">Цена</th>      
      <th class="" scope="col">Описание</th>
      <th class="" scope="col">Последнее обновление</th>
      <th class="" scope="col">Дата создания</th>
      <th class="shrink" scope="col"></th>
    </tr>
  </thead>
  <tbody>
  	<% @services.each do |service| %>
    <tr>
      <td><%= service.id %></td>
      <td><%= service.name %></td>
      <td><%= service.price %></td>
      <td><%= service.description %></td>
      <td><%= service.updated_at %></td>
      <td><%= service.created_at %></td>
      <td><div class="btn-group btn-group-sm align-content-start" role="group"><%= link_to(admin_service_path(service), title: "Показать", class: "btn btn-outline-primary", data: { toggle: 'tooltip', placement: 'top', original_title: 'Показать' }) do %>
          <%= icon("info-circle", class: "bi bi-info-circle") %>
          <% end %>

          <%= link_to(edit_admin_service_path(service), title: "Редактировать", class: "btn btn-outline-warning", data: { toggle: 'tooltip', placement: 'top', original_title: 'Редактировать' }) do %>
          <%= icon("pencil-square", class: "bi bi-pencil-square") %>
          <% end %>

          <%= link_to(admin_service_path(service), method: :delete, title: "Удалить", class: "btn btn-outline-danger", data: { toggle: 'tooltip', placement: 'top', original_title: 'Удалить', confirm: 'Вы действительно хотите удалить объект?' }) do %>
          <%= icon("backspace-reverse", class: "bi bi-bi bi-backspace-reverse") %>
          <% end %>
        </div>
      </td>
    </tr>
    <% end %>
  </tbody>
</table>
</div>